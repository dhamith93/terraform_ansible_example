[db_server]
${db_ip} ansible_user=centos ansible_ssh_private_key_file=${key_path}

[uat_server]
${uat_ip} ansible_user=centos ansible_ssh_private_key_file=${key_path}

[prod_server]
${prod_ip} ansible_user=centos ansible_ssh_private_key_file=${key_path}

[demo_server]
${demo_ip} ansible_user=centos ansible_ssh_private_key_file=${key_path}